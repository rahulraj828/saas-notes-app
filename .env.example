# Copy this file to .env and fill values before running locally or deploying to Vercel
# MongoDB connection string (replace with your Atlas URI)
MONGO_URI=mongodb+srv://<user>:<password>@cluster0.mongodb.net/saas_notes_db?retryWrites=true&w=majority

# JWT secret used to sign tokens
JWT_SECRET=replace_this_with_a_strong_secret
